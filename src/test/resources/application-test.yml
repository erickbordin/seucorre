spring:
  cache:
    type: none
  jpa:
    hibernate:
      ddl-auto: validate
  data:
    redis:
      host: localhost
      port: 6379
  flyway:
    enabled: true
  ai:
    anthropic:
      api-key: mock-key

api:
  security:
    token:
      secret: seucorre_integration_test_secret

seucorre:
  treino:
    geracao:
      mode: preset
  ia:
    provider: anthropic
